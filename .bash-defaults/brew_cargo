# bash profile library for cargo installed packages (rust)
# namespace: cargo

function env::brew-cargo {
    env::brew  # dependency
    # brew install rust
    # e.g. cargo install names
    export PATH="${HOME}/.cargo/bin:$PATH"
    PS1="${PS1}[cargo] "
}
