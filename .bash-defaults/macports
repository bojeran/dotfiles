# bash profile library for macports installed packages
# namespace: ports

function env::macports {
    # prevent multiple imports
    if which macports &>/dev/null; then return; fi

    export PATH="/opt/local/bin:/opt/local/sbin:$PATH"
    PS1="${PS1}[macports] "
}
